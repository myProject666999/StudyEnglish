<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.english.mapper.BookMapper">

    <resultMap id="BaseResultMap" type="com.english.entity.Book">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="author" property="author"/>
        <result column="description" property="description"/>
        <result column="cover_url" property="coverUrl"/>
        <result column="category" property="category"/>
        <result column="difficulty" property="difficulty"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, author, description, cover_url, category, difficulty, status, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_book
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_book
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_book
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') 
            OR author LIKE CONCAT('%', #{keyword}, '%')
            OR description LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.english.entity.Book" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_book (title, author, description, cover_url, category, difficulty, status)
        VALUES (#{title}, #{author}, #{description}, #{coverUrl}, #{category}, #{difficulty}, #{status})
    </insert>

    <update id="update" parameterType="com.english.entity.Book">
        UPDATE t_book
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="author != null">author = #{author},</if>
            <if test="description != null">description = #{description},</if>
            <if test="coverUrl != null">cover_url = #{coverUrl},</if>
            <if test="category != null">category = #{category},</if>
            <if test="difficulty != null">difficulty = #{difficulty},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_book WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM t_book WHERE status = 1
    </select>

</mapper>
